name: Build and Push Docker Image

on:
  push:
    branches:
      - main
      - develop
      - backup-pulseaudio-implementation
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: write
  id-token: write  # Required for Azure OIDC and git tag push

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      meeting_bot_digest: ${{ steps.docker_build.outputs.digest }}
      meeting_bot_tags: ${{ steps.meta.outputs.tags }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: azure/login@v2
        with:
          client-id: ${{ github.ref_name == 'main' && vars.AZURE_CLIENT_ID_PROD || vars.AZURE_CLIENT_ID_DEV }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Set ACR Registry URL
        id: acr
        run: |
          if [[ "${{ github.ref_name }}" == "main" ]]; then
            echo "REGISTRY=${{ vars.ACR_LOGIN_SERVER_PROD }}" >> $GITHUB_ENV
          else
            echo "REGISTRY=${{ vars.ACR_LOGIN_SERVER_DEV }}" >> $GITHUB_ENV
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Azure Container Registry
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}

      # Get version/tag only on main push
      - name: Get version from package.json
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: pkg
        run: |
          VERSION=$(jq -r .version package.json)
          TAG="v$VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "TAG=$TAG" >> $GITHUB_ENV
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      # Check if tag exists already (only on main push)
      - name: Check if tag exists
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: check_tag
        run: |
          if git ls-remote --tags origin "refs/tags/${{ env.TAG }}" | grep .; then
            echo "Tag ${{ env.TAG }} already exists — skipping tag creation."
            echo "tag_exists=true" >> $GITHUB_OUTPUT
          else
            echo "Tag ${{ env.TAG }} does not exist — will create."
            echo "tag_exists=false" >> $GITHUB_OUTPUT
          fi

      # Create and push git tag if it doesn't exist (only on main push)
      - name: Create and push tag
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.check_tag.outputs.tag_exists != 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag ${{ env.TAG }}
          git push origin ${{ env.TAG }}

      # Always generate Docker tags for build, but only include version tag on main
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/meeting-bot
          tags: |
            ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && format('type=raw,value={0}', env.TAG) || '' }}
            type=ref,event=branch
            type=sha,prefix=sha-
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      # Build (always), push only on main or develop
      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.production
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Scan meeting-bot image with Grype (ADV-894)
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: anchore/scan-action@1638637db639e0ade3258b51db49a9a137574c3e # v6
        with:
          image: ${{ env.REGISTRY }}/meeting-bot@${{ steps.docker_build.outputs.digest }}
          fail-build: true
          severity-cutoff: high

      - name: Output Meeting-Bot Image Reference
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        run: |
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 MEETING-BOT IMAGE BUILT AND PUSHED"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Git SHA: $(git rev-parse --short=8 HEAD)"
          echo "Branch:  ${{ github.ref_name }}"
          echo ""
          echo "Image Digest: ${{ steps.docker_build.outputs.digest }}"
          echo ""
          echo "Available Tags:"
          echo "${{ steps.meta.outputs.tags }}" | while read tag; do
            if [[ -n "$tag" ]]; then
              echo "  • $tag"
            fi
          done
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔗 COPY THIS FOR TERRAFORM/KUBERNETES:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          FIRST_TAG=$(echo "${{ steps.meta.outputs.tags }}" | head -n1)
          REGISTRY=$(echo "$FIRST_TAG" | cut -d'/' -f1)
          REPO=$(echo "$FIRST_TAG" | cut -d':' -f1 | cut -d'/' -f2-)
          echo ""
          echo "${REGISTRY}/${REPO}@${{ steps.docker_build.outputs.digest }}"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""


  build-manager:
    runs-on: ubuntu-latest
    outputs:
      manager_digest: ${{ steps.manager_docker_build.outputs.digest }}
      manager_tags: ${{ steps.manager_meta.outputs.tags }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: azure/login@v2
        with:
          client-id: ${{ github.ref_name == 'main' && vars.AZURE_CLIENT_ID_PROD || vars.AZURE_CLIENT_ID_DEV }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Set ACR Registry URL
        id: manager_acr
        run: |
          if [[ "${{ github.ref_name }}" == "main" ]]; then
            echo "REGISTRY=${{ vars.ACR_LOGIN_SERVER_PROD }}" >> $GITHUB_ENV
          else
            echo "REGISTRY=${{ vars.ACR_LOGIN_SERVER_DEV }}" >> $GITHUB_ENV
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Azure Container Registry
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}

      # Get version/tag only on main push
      - name: Get version from package.json
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: pkg
        run: |
          VERSION=$(jq -r .version package.json)
          TAG="v$VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "TAG=$TAG" >> $GITHUB_ENV
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Extract metadata for manager
        id: manager_meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/manager
          tags: |
            ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && format('type=raw,value={0}', env.TAG) || '' }}
            type=ref,event=branch
            type=sha,prefix=sha-
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push manager Docker image
        id: manager_docker_build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.manager
          platforms: ${{ github.event_name == 'pull_request' && 'linux/amd64' || 'linux/amd64,linux/arm64' }}
          push: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop') }}
          # For PRs we don't push; load the first platform into the runner's
          # Docker engine so the smoke test can run.
          load: ${{ github.event_name == 'pull_request' }}
          tags: ${{ steps.manager_meta.outputs.tags }}
          labels: ${{ steps.manager_meta.outputs.labels }}
          cache-from: type=gha,scope=manager
          cache-to: type=gha,mode=max,scope=manager
          build-args: |
            WHISPER_CPP_ENABLE_CUDA=1

      # PR-only: run a cheap smoke check to catch dependency mismatches
      # (e.g., SpeechBrain <-> huggingface_hub API incompatibilities) without
      # requiring GCS or running whisper/diarization.
      - name: Smoke test manager image (imports + versions)
        if: github.event_name == 'pull_request'
        run: |
          # Use the first tag produced by metadata-action (and applied by the
          # build step). With load=true on PRs, this tag exists locally.
          IMAGE=$(echo "${{ steps.manager_meta.outputs.tags }}" | head -n1)
          echo "Running smoke test against $IMAGE"
          docker run --rm "$IMAGE" python - <<'PY'
          import importlib
          import pkgutil
          
          # Ensure our offline pipeline module is importable
          import offline_pipeline  # noqa: F401
          
          # Ensure SpeechBrain and HF hub are present and compatible
          import speechbrain  # noqa: F401
          import huggingface_hub
          
          print('speechbrain', speechbrain.__version__)
          print('huggingface_hub', huggingface_hub.__version__)
          
          # Quick sanity: signature still contains a token parameter name that
          # SpeechBrain 1.0.x expects (use_auth_token). If not, we fail fast.
          from huggingface_hub import hf_hub_download
          import inspect
          params = inspect.signature(hf_hub_download).parameters
          if 'use_auth_token' not in params and 'token' not in params:
            raise SystemExit('hf_hub_download has no auth token param? Unexpected signature.')
          
          # Confirm whisper-cli exists in image (offline mode expects it)
          import os
          from pathlib import Path
          whisper_cli = Path('/app/tools/whisper.cpp/build/bin/whisper-cli')
          if not whisper_cli.exists():
            raise SystemExit(f'missing whisper-cli at {whisper_cli}')
          
          # Confirm whisper model exists
          model = Path('/app/tools/whisper.cpp/models/ggml-base.en.bin')
          if not model.exists():
            raise SystemExit(f'missing whisper model at {model}')
          
          print('manager smoke check: OK')
          PY

      - name: Scan manager image with Grype (ADV-894)
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: anchore/scan-action@1638637db639e0ade3258b51db49a9a137574c3e # v6
        with:
          image: ${{ env.REGISTRY }}/manager@${{ steps.manager_docker_build.outputs.digest }}
          fail-build: true
          severity-cutoff: high

      - name: Output Manager Image Reference
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        run: |
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 MANAGER IMAGE BUILT AND PUSHED"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Git SHA: $(git rev-parse --short=8 HEAD)"
          echo "Branch:  ${{ github.ref_name }}"
          echo ""
          echo "Image Digest: ${{ steps.manager_docker_build.outputs.digest }}"
          echo ""
          echo "Available Tags:"
          echo "${{ steps.manager_meta.outputs.tags }}" | while read tag; do
            if [[ -n "$tag" ]]; then
              echo "  • $tag"
            fi
          done
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔗 COPY THIS FOR TERRAFORM/KUBERNETES:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          FIRST_TAG=$(echo "${{ steps.manager_meta.outputs.tags }}" | head -n1)
          REGISTRY=$(echo "$FIRST_TAG" | cut -d'/' -f1)
          REPO=$(echo "$FIRST_TAG" | cut -d':' -f1 | cut -d'/' -f2-)
          echo ""
          echo "${REGISTRY}/${REPO}@${{ steps.manager_docker_build.outputs.digest }}"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""


  build-controller:
    runs-on: ubuntu-latest
    outputs:
      controller_digest: ${{ steps.controller_docker_build.outputs.digest }}
      controller_tags: ${{ steps.controller_meta.outputs.tags }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: azure/login@v2
        with:
          client-id: ${{ github.ref_name == 'main' && vars.AZURE_CLIENT_ID_PROD || vars.AZURE_CLIENT_ID_DEV }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Set ACR Registry URL
        id: controller_acr
        run: |
          if [[ "${{ github.ref_name }}" == "main" ]]; then
            echo "REGISTRY=${{ vars.ACR_LOGIN_SERVER_PROD }}" >> $GITHUB_ENV
          else
            echo "REGISTRY=${{ vars.ACR_LOGIN_SERVER_DEV }}" >> $GITHUB_ENV
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Azure Container Registry
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}

      # Get version/tag only on main push
      - name: Get version from package.json
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: pkg
        run: |
          VERSION=$(jq -r .version package.json)
          TAG="v$VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "TAG=$TAG" >> $GITHUB_ENV
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      # Generate Docker tags for controller
      - name: Extract metadata for controller
        id: controller_meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/controller
          tags: |
            ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && format('type=raw,value={0}', env.TAG) || '' }}
            type=ref,event=branch
            type=sha,prefix=sha-
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      # Build controller image (always), push only on main or develop
      - name: Build and push controller Docker image
        id: controller_docker_build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.controller
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop') }}
          tags: ${{ steps.controller_meta.outputs.tags }}
          labels: ${{ steps.controller_meta.outputs.labels }}
          cache-from: type=gha,scope=controller
          cache-to: type=gha,mode=max,scope=controller

      - name: Scan controller image with Grype (ADV-894)
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        uses: anchore/scan-action@1638637db639e0ade3258b51db49a9a137574c3e # v6
        with:
          image: ${{ env.REGISTRY }}/controller@${{ steps.controller_docker_build.outputs.digest }}
          fail-build: true
          severity-cutoff: high

      - name: Output Controller Image Reference
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
        run: |
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 CONTROLLER IMAGE BUILT AND PUSHED"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "Git SHA: $(git rev-parse --short=8 HEAD)"
          echo "Branch:  ${{ github.ref_name }}"
          echo ""
          echo "Image Digest: ${{ steps.controller_docker_build.outputs.digest }}"
          echo ""
          echo "Available Tags:"
          echo "${{ steps.controller_meta.outputs.tags }}" | while read tag; do
            if [[ -n "$tag" ]]; then
              echo "  • $tag"
            fi
          done
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔗 COPY THIS FOR TERRAFORM/KUBERNETES:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          FIRST_TAG=$(echo "${{ steps.controller_meta.outputs.tags }}" | head -n1)
          REGISTRY=$(echo "$FIRST_TAG" | cut -d'/' -f1)
          REPO=$(echo "$FIRST_TAG" | cut -d':' -f1 | cut -d'/' -f2-)
          echo ""
          echo "${REGISTRY}/${REPO}@${{ steps.controller_docker_build.outputs.digest }}"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

  summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs: [build, build-controller, build-manager]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Display Build Summary
        run: |
          echo ""
          echo "╔═══════════════════════════════════════════════════════════════════════════╗"
          echo "║                    🎉 BUILD COMPLETE - IMAGE SUMMARY                      ║"
          echo "╚═══════════════════════════════════════════════════════════════════════════╝"
          echo ""
          echo "Branch:    ${{ github.ref_name }}"
          echo "Commit:    $(git rev-parse --short=8 HEAD)"
          echo "Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "Repository: meeting-bot (consolidated)"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 COPY THESE REFERENCES FOR TERRAFORM"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "controller:"
          CONTROLLER_TAG=$(echo "${{ needs.build-controller.outputs.controller_tags }}" | head -n1)
          CONTROLLER_REGISTRY=$(echo "$CONTROLLER_TAG" | cut -d'/' -f1)
          CONTROLLER_REPO=$(echo "$CONTROLLER_TAG" | cut -d':' -f1 | cut -d'/' -f2-)
          echo "  ${CONTROLLER_REGISTRY}/${CONTROLLER_REPO}@${{ needs.build-controller.outputs.controller_digest }}"
          echo ""
          echo "manager:"
          MANAGER_TAG=$(echo "${{ needs.build-manager.outputs.manager_tags }}" | head -n1)
          MANAGER_REGISTRY=$(echo "$MANAGER_TAG" | cut -d'/' -f1)
          MANAGER_REPO=$(echo "$MANAGER_TAG" | cut -d':' -f1 | cut -d'/' -f2-)
          echo "  ${MANAGER_REGISTRY}/${MANAGER_REPO}@${{ needs.build-manager.outputs.manager_digest }}"
          echo ""
          echo "meeting-bot:"
          MEETINGBOT_TAG=$(echo "${{ needs.build.outputs.meeting_bot_tags }}" | head -n1)
          MEETINGBOT_REGISTRY=$(echo "$MEETINGBOT_TAG" | cut -d'/' -f1)
          MEETINGBOT_REPO=$(echo "$MEETINGBOT_TAG" | cut -d':' -f1 | cut -d'/' -f2-)
          echo "  ${MEETINGBOT_REGISTRY}/${MEETINGBOT_REPO}@${{ needs.build.outputs.meeting_bot_digest }}"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "✅ All three images built and pushed successfully!"
          echo ""
